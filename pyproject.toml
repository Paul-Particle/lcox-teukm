[project]
name = "lcox-teukm"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "kaleido>=1.3.0",
    "numpy>=2.4.6",
    "plotly>=6.8.0",
    "pyarrow>=24.0.0",
    "pyyaml>=6.0.3",
]
